## frozen mockup — 2026-05-16 build workspace

this directory is a verbatim snapshot of `src/App.tsx`, `src/App.css`,
`src/App.test.tsx`, `src/core/`, and `src/workspaces/` as of 2026-05-16.

**precedence:** when the master implementation plan, the prd
(`docs/specs/2026-05-14-operator-ide-prd.md`), or `docs/ui-direction.md`
conflict with what is rendered by this mockup, the mockup wins. drift is
a documentation bug, not a mockup bug.

do not edit files in this directory. they exist so future passes can
diff the implementation against the agreed visual and interaction target.
